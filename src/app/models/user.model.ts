export interface User {
  id: string; // uuid
  email: string;
  //name: string;
  password: string; // según tu petición: sin restricciones, max 15 caracteres
  //createdAt: string;
  //updatedAt: string;
}
