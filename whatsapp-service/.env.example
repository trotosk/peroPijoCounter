# ID del marcador en PeroPijoCounter (el de 8 caracteres que aparece en la app)
COUNTER_ID=XXXXXXXX

# Nombre exacto del grupo de WhatsApp (tal como aparece en la app)
GROUP_NAME=Padres Voleibol U12

# Tiempo mínimo en milisegundos entre mensajes (por defecto 30000 = 30 segundos)
# Sube este valor si quieres menos mensajes (ej: 60000 = 1 minuto)
MIN_INTERVAL_MS=30000
